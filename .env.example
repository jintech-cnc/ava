# ============================================================
# Ava — Variables d'environnement (exemple)
# Copie ce fichier en .env et remplis les valeurs réelles
# ============================================================

# --- Sécurité Django ----------------------------------------
# Génère une clé : python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-CHANGEZ-MOI-EN-PRODUCTION-svp

# Mettre à False en production
DEBUG=True

# Hosts autorisés (séparés par virgule)
ALLOWED_HOSTS=localhost,127.0.0.1

# --- Base de données Neon PostgreSQL -----------------------
# Connection string de ton projet Neon :
# postgresql://neondb_owner:npg_3GRYc5pXThbJ@ep-green-boat-aefnperc-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
DATABASE_URL=postgresql://neondb_owner:npg_3GRYc5pXThbJ@ep-green-boat-aefnperc-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# --- Superuser (créé automatiquement au build) -----------
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@ava.local
DJANGO_SUPERUSER_PASSWORD=ChangeMoi2026!
DJANGO_SUPERUSER_FIRST_NAME=Admin
DJANGO_SUPERUSER_LAST_NAME=Ava

# --- Email (optionnel — console par défaut) ---------------
# Pour production, configure un vrai SMTP (SendGrid, Mailgun, etc.)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# DEFAULT_FROM_EMAIL=noreply@ava.local

# Contact affiché sur la page de mot de passe oublié
ADMIN_CONTACT_EMAIL=admin@ava.local
